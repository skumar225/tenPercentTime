// Given a number, we need to find sum of its digits using recursion.

// Examples:

// Input : 12345
// Output : 15

// Input : 45632
// Output : 20


/* 
-- % 10 returns the remainder, and its the last number of the number
-- Divide a number by 10, it takes the decimal point to one spot over, and Math.floor rounds it down to get the remaining number

*/

function recurseIt(number) {
  

  if(number ==== 0) {
    return number;
  }
// What is modulus 10? Why does it count down for us? 
    else {
	var last =
	var rest =
	   
    }

}
