//Functions nasics - stored in vars, returned from other functions, standalone

function square(num) {

    return num*num;
}

square(5); // retuns 25

var square = function (num) {
    returns n * n;
}

// Returns SQUARE IS NOT A FUNCTION
// the var will be hoisted and will not be executed until JS encounters its definition;


